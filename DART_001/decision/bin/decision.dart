void main() {
  int idade = 16;

  // Podemos usar o if - else + operadores lógicos para fornecer instruções de decisão.
  if (idade >= 20) {
    print('É maior de idade.');
  } else {
    print('É menor de idade');
  }
}
